{
    "private": true,
    "scripts": {
        "build": "spago build",
        "example": "spago build --path 'example/src/*.purs' && spago bundle-app --to example/dist/app.js",
        "docs":
            "purs docs --format html \"src/**/*.purs\""
    },
    "dependencies": {
    },
    "devDependencies": {
    }
}
